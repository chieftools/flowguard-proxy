{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://pkg.flowguard.network/config.schema.json",
    "type": "object",
    "title": "FlowGuard Proxy Configuration",
    "description": "Configuration schema for the FlowGuard reverse proxy.",
    "properties": {
        "$schema": {
            "type": "string",
            "description": "JSON Schema reference for IDE validation and autocomplete"
        },
        "id": {
            "type": "string",
            "format": "ulid",
            "description": "Unique identifier for this configuration version"
        },
        "host": {
            "$ref": "#/definitions/host_config"
        },
        "rules": {
            "type": "object",
            "description": "Security rules that define when to take actions",
            "patternProperties": {
                "^[a-zA-Z0-9_-]+$": {
                    "$ref": "#/definitions/rule"
                }
            },
            "additionalProperties": false
        },
        "server": {
            "$ref": "#/definitions/server_config"
        },
        "actions": {
            "type": "object",
            "description": "Actions that can be taken when rules match",
            "patternProperties": {
                "^[a-zA-Z0-9_-]+$": {
                    "$ref": "#/definitions/action"
                }
            },
            "additionalProperties": false
        },
        "logging": {
            "$ref": "#/definitions/logger_config"
        },
        "updates": {
            "$ref": "#/definitions/updates_config"
        },
        "firewall": {
            "$ref": "#/definitions/firewall_config"
        },
        "ip_lists": {
            "$ref": "#/definitions/ip_lists_config"
        },
        "realtime": {
            "$ref": "#/definitions/realtime_config"
        },
        "heartbeat": {
            "$ref": "#/definitions/heartbeat_config"
        },
        "challenges": {
            "$ref": "#/definitions/challenges_config"
        },
        "ip_database": {
            "$ref": "#/definitions/ip_database_config"
        },
        "trusted_proxies": {
            "$ref": "#/definitions/trusted_proxies_config"
        }
    },
    "definitions": {
        "host_config": {
            "type": "object",
            "description": "Host and API configuration",
            "properties": {
                "id": {
                    "type": "string",
                    "format": "ulid",
                    "description": "The FlowGuard server identifier"
                },
                "key": {
                    "type": "string",
                    "description": "The FlowGuard API key for authenticating requests"
                },
                "team": {
                    "type": "string",
                    "description": "The FlowGuard team slug this host belongs to"
                },
                "name": {
                    "type": "string",
                    "description": "The human-readable name of this host"
                },
                "cache_path": {
                    "type": "string",
                    "description": "Path to directory for caching remote resources (IP databases, IP lists, etc.)"
                },
                "cert_path": {
                    "type": "string",
                    "description": "Path to directory containing combined SSL certificate+key files. Optional - can be used alone or with nginx_config_path."
                },
                "nginx_config_path": {
                    "type": "string",
                    "description": "Path to NGINX configuration file to parse for SSL certificates. Optional - can be used alone or with cert_path."
                },
                "default_hostname": {
                    "type": "string",
                    "description": "Default hostname to use when a certificate is not found"
                }
            },
            "additionalProperties": false
        },
        "server_config": {
            "type": "object",
            "description": "Proxy server runtime configuration",
            "properties": {
                "protocols": {
                    "$ref": "#/definitions/protocols_config"
                },
                "advertise_http3": {
                    "type": "boolean",
                    "description": "Advertise HTTP/3 availability on HTTPS responses by adding an Alt-Svc header when HTTP/3 is enabled"
                }
            },
            "additionalProperties": false
        },
        "protocols_config": {
            "type": "object",
            "description": "HTTP protocols enabled by the proxy. Omitted values default to true.",
            "properties": {
                "http1": {
                    "type": "boolean",
                    "description": "Enable HTTP/1.0 and HTTP/1.1 over cleartext HTTP and TLS"
                },
                "http2": {
                    "type": "boolean",
                    "description": "Enable HTTP/2 over TLS"
                },
                "http3": {
                    "type": "boolean",
                    "description": "Enable HTTP/3 over QUIC/UDP"
                }
            },
            "additionalProperties": false
        },
        "rule": {
            "type": "object",
            "description": "A security rule with conditions and an action",
            "properties": {
                "name": {
                    "type": "string",
                    "description": "The human-readable name of this rule"
                },
                "action": {
                    "type": "string",
                    "description": "The action ID to execute when this rule matches"
                },
                "sort_order": {
                    "type": "integer",
                    "description": "Optional explicit ordering for rule processing. Lower values are processed first, including 0. Rules without sort_order are processed after ordered rules, sorted by rule ID."
                },
                "conditions": {
                    "$ref": "#/definitions/conditions"
                }
            },
            "required": [
                "action",
                "conditions"
            ],
            "additionalProperties": false
        },
        "conditions": {
            "type": "object",
            "description": "Logical conditions for rule matching",
            "properties": {
                "operator": {
                    "type": "string",
                    "enum": [
                        "AND",
                        "OR",
                        "NAND",
                        "NOR"
                    ],
                    "description": "Logical operator for combining conditions (default: AND)"
                },
                "groups": {
                    "type": "array",
                    "description": "Nested condition groups",
                    "items": {
                        "$ref": "#/definitions/conditions"
                    }
                },
                "matches": {
                    "type": "array",
                    "description": "Match conditions to evaluate",
                    "items": {
                        "$ref": "#/definitions/match"
                    }
                },
                "comment": {
                    "type": "string",
                    "description": "Optional comment explaining this condition group"
                }
            },
            "additionalProperties": false
        },
        "match": {
            "type": "object",
            "description": "A single match condition",
            "properties": {
                "type": {
                    "type": "string",
                    "enum": [
                        "domain",
                        "path",
                        "header",
                        "user-agent",
                        "ip",
                        "asn",
                        "as-name",
                        "as-domain",
                        "country",
                        "continent",
                        "ipset",
                        "iplist",
                        "registerable-domain",
                        "fingerprint-ja4"
                    ],
                    "description": "The type of value to match against"
                },
                "match": {
                    "type": "string",
                    "enum": [
                        "equals",
                        "not-equals",
                        "contains",
                        "not-contains",
                        "starts-with",
                        "not-starts-with",
                        "ends-with",
                        "not-ends-with",
                        "in",
                        "not-in",
                        "regex",
                        "not-regex",
                        "exists",
                        "missing"
                    ],
                    "description": "The matching operation to perform"
                },
                "key": {
                    "type": "string",
                    "description": "The header name to match against. Required for 'header' type matches."
                },
                "value": {
                    "type": "string",
                    "description": "The value to match against. Required for most operations except 'in'/'not-in' (use 'values' instead) and 'exists'/'missing'."
                },
                "values": {
                    "type": "array",
                    "items": {
                        "type": "string"
                    },
                    "description": "List of values for 'in' and 'not-in' operations. Required when match is 'in' or 'not-in'."
                },
                "case_insensitive": {
                    "type": "boolean",
                    "description": "Perform case-insensitive matching (default: false)"
                },
                "raw_match": {
                    "type": "boolean",
                    "description": "Skip URL normalization for path matching to detect malformed URLs (default: false). Only applies to 'path' type matches."
                },
                "confidence": {
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 100,
                    "description": "Minimum confidence level (0-100) required for a match to be considered valid (only applies to IP list matches)"
                },
                "family": {
                    "type": "integer",
                    "enum": [
                        4,
                        6
                    ],
                    "description": "IP family for ipset matches (4 for IPv4, 6 for IPv6)"
                }
            },
            "required": [
                "type",
                "match"
            ],
            "additionalProperties": false,
            "allOf": [
                {
                    "if": {
                        "properties": {
                            "type": {
                                "const": "header"
                            }
                        }
                    },
                    "then": {
                        "required": [
                            "key"
                        ]
                    }
                }
            ]
        },
        "action": {
            "type": "object",
            "description": "An action to take when a rule matches",
            "properties": {
                "name": {
                    "type": "string",
                    "description": "The human-readable name of this action"
                },
                "action": {
                    "type": "string",
                    "enum": [
                        "log",
                        "allow",
                        "block",
                        "rate_limit",
                        "challenge"
                    ],
                    "description": "The type of action to take."
                },
                "status": {
                    "type": "integer",
                    "minimum": 100,
                    "maximum": 599,
                    "description": "HTTP status code to return"
                },
                "message": {
                    "type": "string",
                    "description": "Response message to send"
                },
                "requests_per_window": {
                    "type": "integer",
                    "minimum": 1,
                    "description": "Maximum number of requests allowed in the time window (for rate_limit actions)"
                },
                "window_seconds": {
                    "type": "integer",
                    "minimum": 1,
                    "description": "Time window in seconds for rate limiting (for rate_limit actions)"
                },
                "challenge": {
                    "$ref": "#/definitions/action_challenge_config"
                }
            },
            "required": [
                "action"
            ],
            "additionalProperties": false,
            "allOf": [
                {
                    "if": {
                        "properties": {
                            "action": {
                                "const": "rate_limit"
                            }
                        }
                    },
                    "then": {
                        "required": [
                            "requests_per_window",
                            "window_seconds"
                        ]
                    }
                },
                {
                    "if": {
                        "properties": {
                            "action": {
                                "const": "challenge"
                            }
                        }
                    },
                    "then": {
                        "properties": {
                            "challenge": {
                                "$ref": "#/definitions/action_challenge_config"
                            }
                        }
                    }
                }
            ]
        },
        "action_challenge_config": {
            "type": "object",
            "description": "Challenge behavior for challenge actions. V1 supports built-in proof-of-work only.",
            "properties": {
                "type": {
                    "type": "string",
                    "enum": [
                        "pow"
                    ],
                    "description": "Challenge type. Defaults to pow."
                },
                "clearance_scope": {
                    "type": "string",
                    "enum": [
                        "rule",
                        "host"
                    ],
                    "description": "Scope cleared by a successful challenge. Defaults to rule. Use host to clear other challenge rules on the same request host."
                },
                "ttl_seconds": {
                    "type": "integer",
                    "minimum": 0,
                    "description": "Clearance cookie lifetime for this action. Defaults to challenges.default_ttl_seconds. Set to 0 for a session clearance cookie with no signed clearance expiry."
                },
                "min_page_time_ms": {
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 60000,
                    "description": "Minimum time in milliseconds the challenge page must exist before verification is accepted. Defaults to challenges.min_page_time_ms. Set to 0 to disable the dwell-time gate."
                },
                "difficulty_bits": {
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 30,
                    "description": "Leading-zero difficulty for probabilistic proof-of-work. In calibrated mode, this derives work_units when work_units is omitted. Defaults to challenges.pow.difficulty_bits."
                },
                "algorithm": {
                    "type": "string",
                    "enum": [
                        "pbkdf2-sha256",
                        "sha256"
                    ],
                    "description": "Proof-of-work algorithm for this action. Defaults to challenges.pow.algorithm."
                },
                "pbkdf2_iterations": {
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 100000,
                    "description": "PBKDF2 iteration count for this action when algorithm is pbkdf2-sha256. Defaults to challenges.pow.pbkdf2_iterations."
                },
                "effort_mode": {
                    "type": "string",
                    "enum": [
                        "calibrated",
                        "probabilistic"
                    ],
                    "description": "Proof-of-work effort mode. Calibrated uses a fixed work-unit count for predictable solve time; probabilistic uses leading-zero difficulty. Defaults to challenges.pow.effort_mode."
                },
                "work_units": {
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 100000,
                    "description": "Fixed sequential work-unit count for calibrated proof-of-work. Defaults to challenges.pow.work_units, or is derived from difficulty_bits when omitted."
                }
            },
            "additionalProperties": false
        },
        "logger_config": {
            "type": "object",
            "description": "Request logging configuration",
            "properties": {
                "sinks": {
                    "type": "object",
                    "description": "Named logging sinks (destinations). Multiple sinks of the same type can be configured.",
                    "patternProperties": {
                        "^[a-zA-Z0-9_-]+$": {
                            "$ref": "#/definitions/logger_sink"
                        }
                    },
                    "additionalProperties": false
                },
                "header_whitelist": {
                    "type": "array",
                    "description": "List of header names (or prefixes ending with '-') to log values for. If empty, no header values are logged. All header names are always logged regardless of whitelist. The User-Agent header is always logged regardless of this whitelist.",
                    "items": {
                        "type": "string",
                        "examples": [
                            "content-type",
                            "user-agent",
                            "sec-ch-",
                            "cf-"
                        ]
                    }
                }
            },
            "additionalProperties": false
        },
        "logger_sink": {
            "type": "object",
            "description": "A logging sink configuration",
            "properties": {
                "type": {
                    "type": "string",
                    "enum": [
                        "file",
                        "loki",
                        "openobserve"
                    ],
                    "description": "The type of logging sink"
                },
                "path": {
                    "type": "string",
                    "description": "For file sink: path to the log file"
                },
                "url": {
                    "type": "string",
                    "format": "uri",
                    "description": "For loki sink: Loki push API URL (e.g., http://loki:3100/loki/api/v1/push). For openobserve sink: OpenObserve base URL (e.g., https://observe.example.com)"
                },
                "organization": {
                    "type": "string",
                    "description": "For openobserve sink: Organization name in OpenObserve"
                },
                "stream": {
                    "type": "string",
                    "description": "For openobserve sink: Stream name (default: flowguard)"
                },
                "labels": {
                    "type": "object",
                    "description": "For loki sink: Static labels to attach to all log entries",
                    "additionalProperties": {
                        "type": "string"
                    }
                },
                "tenant_id": {
                    "type": "string",
                    "description": "For loki sink: Optional X-Scope-OrgID header for multi-tenancy"
                },
                "username": {
                    "type": "string",
                    "description": "For loki sink: Optional basic auth username. For openobserve sink: Basic auth username (typically email)"
                },
                "password": {
                    "type": "string",
                    "description": "For loki sink: Optional basic auth password. For openobserve sink: Basic auth password (API token)"
                }
            },
            "required": [
                "type"
            ],
            "allOf": [
                {
                    "if": {
                        "properties": {
                            "type": {
                                "const": "file"
                            }
                        }
                    },
                    "then": {
                        "required": [
                            "path"
                        ]
                    }
                },
                {
                    "if": {
                        "properties": {
                            "type": {
                                "const": "loki"
                            }
                        }
                    },
                    "then": {
                        "required": [
                            "url"
                        ]
                    }
                },
                {
                    "if": {
                        "properties": {
                            "type": {
                                "const": "openobserve"
                            }
                        }
                    },
                    "then": {
                        "required": [
                            "url",
                            "organization"
                        ]
                    }
                }
            ],
            "additionalProperties": false
        },
        "updates_config": {
            "type": "object",
            "description": "Configuration for remote package upgrades via WebSocket",
            "properties": {
                "allow_unattended": {
                    "type": "boolean",
                    "description": "Allow the FlowGuard control panel to trigger unattended package upgrades via WebSocket. Default: false"
                }
            },
            "additionalProperties": false
        },
        "firewall_config": {
            "type": "object",
            "description": "Firewall monitoring and repair settings for FlowGuard-managed redirect rules",
            "properties": {
                "monitor_enabled": {
                    "type": "boolean",
                    "description": "Whether FlowGuard should periodically verify its redirect rules while running. Default: true when redirects are managed"
                },
                "auto_repair": {
                    "type": "boolean",
                    "description": "Whether FlowGuard should automatically re-add missing redirect rules. Default: true"
                },
                "check_interval_seconds": {
                    "type": "integer",
                    "minimum": 5,
                    "description": "How often to verify redirect rules in seconds. Default: 30"
                }
            },
            "additionalProperties": false
        },
        "ip_lists_config": {
            "type": "object",
            "description": "In-memory IP list configurations for high-performance IP matching",
            "patternProperties": {
                "^[a-zA-Z0-9_-]+$": {
                    "$ref": "#/definitions/ip_list"
                }
            },
            "additionalProperties": false
        },
        "ip_list": {
            "type": "object",
            "description": "Configuration for a single IP list",
            "properties": {
                "url": {
                    "type": "string",
                    "format": "uri",
                    "description": "URL to fetch IP list from (one IP or CIDR per line)"
                },
                "name": {
                    "type": "string",
                    "description": "Human-readable name for the IP list"
                },
                "path": {
                    "type": "string",
                    "description": "Local file path to load IP list from (one IP or CIDR per line)"
                },
                "confidence": {
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 100,
                    "description": "Minimum confidence level (0-100) for IP list entries contained in this list"
                },
                "refresh_interval_seconds": {
                    "type": "integer",
                    "minimum": 60,
                    "description": "How often to refresh the list from URL in seconds"
                }
            },
            "oneOf": [
                {
                    "required": [
                        "url"
                    ]
                },
                {
                    "required": [
                        "path"
                    ]
                }
            ],
            "additionalProperties": false
        },
        "realtime_config": {
            "type": "object",
            "description": "WebSocket configuration for real-time messaging",
            "properties": {
                "key": {
                    "type": "string",
                    "description": "WebSocket application key"
                },
                "host": {
                    "type": "string",
                    "description": "Host for the WebSocket server"
                },
                "port": {
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 65535,
                    "description": "Port for the WebSocket server"
                },
                "channel": {
                    "type": "string",
                    "description": "Channel name to subscribe to for config update notifications (can be public or private)"
                },
                "auth_url": {
                    "type": "string",
                    "format": "uri",
                    "description": "Authentication URL for private channel access. Required for private channels"
                },
                "encrypted": {
                    "type": "boolean",
                    "description": "Whether to use encrypted WebSocket connections (WSS instead of WS). Default: true"
                }
            },
            "required": [
                "key",
                "host",
                "port",
                "channel"
            ],
            "additionalProperties": false
        },
        "heartbeat_config": {
            "type": "object",
            "description": "Heartbeat configuration for managed proxies to report operational status",
            "properties": {
                "enabled": {
                    "type": "boolean",
                    "description": "Whether heartbeat reporting is enabled. Default: true"
                },
                "interval_seconds": {
                    "type": "integer",
                    "minimum": 30,
                    "description": "How often to send heartbeats in seconds. Default: 300 (5 minutes)"
                },
                "jitter_seconds": {
                    "type": "integer",
                    "minimum": 0,
                    "description": "Random jitter applied as +/- seconds to each interval. Default: 30"
                }
            },
            "additionalProperties": false
        },
        "challenges_config": {
            "type": "object",
            "description": "Global configuration for first-party FlowGuard challenge interstitials.",
            "properties": {
                "cookie_name": {
                    "type": "string",
                    "description": "Name of the HTTP-only clearance cookie. Defaults to fg_clearance."
                },
                "default_ttl_seconds": {
                    "type": "integer",
                    "minimum": 0,
                    "description": "Default clearance lifetime in seconds. Defaults to 1800. Set to 0 for session clearance cookies with no signed clearance expiry."
                },
                "min_page_time_ms": {
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 60000,
                    "description": "Default minimum time in milliseconds the challenge page must exist before verification is accepted. Defaults to 1500. Set to 0 to disable the dwell-time gate."
                },
                "bind_ip": {
                    "type": "boolean",
                    "description": "Bind challenge and clearance tokens to the client IP. Defaults to true."
                },
                "bind_user_agent": {
                    "type": "boolean",
                    "description": "Bind challenge and clearance tokens to the User-Agent. Defaults to true."
                },
                "non_html_status": {
                    "type": "integer",
                    "minimum": 100,
                    "maximum": 599,
                    "description": "Status code for matched non-HTML requests that cannot display an interstitial. Defaults to 202."
                },
                "max_attempts_per_window": {
                    "type": "integer",
                    "minimum": 0,
                    "description": "Maximum failed or successful verification attempts per client/window. Defaults to 20. Set to 0 to disable challenge attempt limiting."
                },
                "attempt_window_seconds": {
                    "type": "integer",
                    "minimum": 1,
                    "description": "Attempt limit window in seconds. Defaults to 60."
                },
                "secret": {
                    "type": "string",
                    "description": "Optional HMAC secret for challenge and clearance tokens. Defaults to host.key; otherwise a process-local key is generated."
                },
                "pow": {
                    "$ref": "#/definitions/pow_challenge_config"
                }
            },
            "additionalProperties": false
        },
        "pow_challenge_config": {
            "type": "object",
            "description": "Default settings for FlowGuard's built-in proof-of-work challenge.",
            "properties": {
                "challenge_ttl_seconds": {
                    "type": "integer",
                    "minimum": 1,
                    "description": "Signed proof-of-work challenge token lifetime in seconds. Defaults to 120."
                },
                "difficulty_bits": {
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 30,
                    "description": "Default leading-zero difficulty for probabilistic proof-of-work. In calibrated mode, this derives work_units when work_units is omitted. Defaults to 18."
                },
                "algorithm": {
                    "type": "string",
                    "enum": [
                        "pbkdf2-sha256",
                        "sha256"
                    ],
                    "description": "Default proof-of-work algorithm. Defaults to pbkdf2-sha256."
                },
                "pbkdf2_iterations": {
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 100000,
                    "description": "Default PBKDF2 iteration count. Defaults to 100."
                },
                "effort_mode": {
                    "type": "string",
                    "enum": [
                        "calibrated",
                        "probabilistic"
                    ],
                    "description": "Default proof-of-work effort mode. Defaults to calibrated."
                },
                "work_units": {
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 100000,
                    "description": "Default fixed sequential work-unit count for calibrated proof-of-work. If omitted, FlowGuard derives it from difficulty_bits; difficulty 18 maps to 8192 and high values clamp at 100000."
                }
            },
            "additionalProperties": false
        },
        "ip_database_config": {
            "type": "object",
            "description": "IP geolocation database configuration",
            "properties": {
                "url": {
                    "type": "string",
                    "format": "uri",
                    "description": "URL to download the IP database from"
                },
                "refresh_interval_seconds": {
                    "type": "integer",
                    "minimum": 60,
                    "description": "How often to refresh the database in seconds"
                }
            },
            "required": [
                "url"
            ],
            "additionalProperties": false
        },
        "trusted_proxies_config": {
            "type": "object",
            "description": "Trusted proxy configuration for X-Forwarded-For processing",
            "properties": {
                "ipnets": {
                    "type": "array",
                    "description": "List of trusted proxy networks (CIDR notation or URLs)",
                    "minItems": 1,
                    "items": {
                        "type": "string",
                        "oneOf": [
                            {
                                "pattern": "^https?://",
                                "description": "URL to fetch IP ranges from"
                            },
                            {
                                "pattern": "^([0-9]{1,3}\\.){3}[0-9]{1,3}(/[0-9]{1,2})?$",
                                "description": "IPv4 address or CIDR"
                            },
                            {
                                "pattern": "^[0-9a-fA-F:]+(/[0-9]{1,3})?$",
                                "description": "IPv6 address or CIDR"
                            }
                        ]
                    }
                },
                "header_auth": {
                    "$ref": "#/definitions/trusted_proxy_header_auth_config"
                },
                "refresh_interval_seconds": {
                    "type": "integer",
                    "minimum": 60,
                    "description": "How often to refresh IP lists from URLs in seconds"
                }
            },
            "anyOf": [
                {
                    "required": [
                        "ipnets"
                    ]
                },
                {
                    "required": [
                        "header_auth"
                    ]
                }
            ],
            "additionalProperties": false
        },
        "trusted_proxy_header_auth_config": {
            "type": "object",
            "description": "Shared-secret header that can authorize trusting forwarded client IP headers",
            "properties": {
                "header": {
                    "type": "string",
                    "minLength": 1,
                    "pattern": "^[!#$%&'*+.^_`|~0-9A-Za-z-]+$",
                    "default": "FG-Trusted-Proxy-Secret",
                    "description": "HTTP header name carrying the trusted proxy shared secret"
                },
                "values": {
                    "type": "array",
                    "minItems": 1,
                    "description": "Allowed shared-secret header values",
                    "items": {
                        "type": "string",
                        "minLength": 1
                    }
                }
            },
            "required": [
                "values"
            ],
            "additionalProperties": false
        }
    },
    "additionalProperties": false
}
